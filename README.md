# github-mcp
MCP server giving Claude direct access to GitHub (repos, files, issues, PRs, Actions) via a personal access token, built to fill the gap where Claude chat has no first party GitHub connector.
